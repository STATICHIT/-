#include<stdio.h>
int main()
{
    int n,i;
    char id[20],in[10],out[10];
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        gets(id);
        gets(in);
        gets(out);
    }

    return 0;
}
